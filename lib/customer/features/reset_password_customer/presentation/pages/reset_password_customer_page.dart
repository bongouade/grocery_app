import 'package:flutter/material.dart';

import 'reset_password_customer_view.dart';

class ResetPasswordCustomerPage extends StatelessWidget {
  const ResetPasswordCustomerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ResetPasswordCustomerView();
  }
}
